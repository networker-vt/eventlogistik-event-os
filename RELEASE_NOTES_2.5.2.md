# Orbit 2.5.2 — release notes

Light-default Home + product rename. GitHub Pages `base` stays `/eventlogistik-event-os/`. Impressum: Mirco Küßner (unchanged). Do not merge until Super veto.

## Super veto checklist

1. **Light theme default** — warm parchment canvas (~`#faf8f5`), dark ink text, restrained teal accents. Dark remains an optional header toggle.
2. **Assist-first above the fold** — greeting + OrbitRobot + one Tageskarte + one filled CTA **Orbit fragen**. No second primary.
3. **Quiet „Mehr entdecken“** below the fold — single-column text-weight links (not filled buttons). NewsStrip lives in this quieter zone. **Kabine is not a second Home product.**
4. **Robot tap** — bounce/blink (`prefers-reduced-motion` static). Exactly **one** time-of-day question (Reise / Job / Frisur / Mitarbeiter / Schuhe), not a menu of five. Answer routes into the right area. Kabine only from that question or Tageskarte.
5. **Renames (DE+EN i18n, nav, document titles, aliases):** Look/Style → **Kabine** · Travel → **Abflug** · Job Match → **Treffer** · B2B hiring → **Crew**. Wallet stays Wallet.

## SoftPaywall

Kabine base (photo + 1–2 variants) stays free. Credits only on extra variants and nearby shop boosts. Robot discovery questions are free (no Assist turn).

## Routes

Legacy `/look`, `/reise`, `/match` still work. Canonical aliases: `/kabine`, `/abflug`, `/treffer`, `/crew`.
